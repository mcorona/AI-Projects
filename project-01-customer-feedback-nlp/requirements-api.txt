# Minimal dependencies to serve src/api.py -- a subset of requirements.txt.
# Used by the Dockerfile so the API image doesn't ship Jupyter, Streamlit,
# pytest, black, etc. Keep these versions in sync with requirements.txt.

torch>=2.0.0
transformers>=4.35.0
pytorch-lightning>=2.1.0
scikit-learn>=1.3.0
numpy>=1.24.0
fastapi>=0.109.0
uvicorn>=0.27.0
